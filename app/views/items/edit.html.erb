<%= simple_form_for [@item.user, @item] do |f| %>
  <%= f.input :name %>
  <%= f.input :price %>
  <%= f.input :brand %>
  <%= f.input :size %>
  <%= f.input :categories %>
  <%= f.submit "Submit updates" %>
  <% end %>

<%= link_to "Back to your items", items_path %>

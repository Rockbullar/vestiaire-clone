<% @items.each do |item| %>
  <%= item.name %>
  <%= item.description %>
  <%= item.price %>
  <%= item.brand %>
  <%= item.is_sold ? "Sold" : "Not sold" %>
  <%= link_to "Details", item_path(item) %>
<% end %>
